{
  "$schema": "https://cursor.com/schemas/environment.schema.json",
  "name": "tech1world-job-apply",
  "install": "cd scripts/job-apply && npm install && npx playwright install chromium --with-deps",
  "start": "Xvfb :99 -screen 0 1920x1080x24 -ac +extension RANDR > /tmp/xvfb.log 2>&1 & export DISPLAY=:99"
}
